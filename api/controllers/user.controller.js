export const test = (req,res)=>{
    res.json({
        message: "hello LUCIEE, isto está a trabalhar!!Mas por alguma razão tens de fazer NPM START, no lado do servidor, para ver, e fazer refresh ao browser..😒"
    })
}
